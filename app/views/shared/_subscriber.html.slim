- if current_user
  - if @subscriber&.persisted?
    p= link_to t('questions.show.unsubscribe'),
            subscriber_path(@subscriber),
            method: :delete,
            remote: true 
  - else
    p= link_to t('questions.show.subscribe'),
            question_subscribers_path(@question),
            method: :post,
            remote: true